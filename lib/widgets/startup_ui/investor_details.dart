import 'package:flutter/material.dart';

class InvestorDetails extends StatelessWidget {
  var startupmodel;
  InvestorDetails({super.key, required startupmodel});
  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
